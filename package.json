{
  "name": "dsh-collab",
  "version": "0.8.4",
  "description": "Multi-agent real-time collaboration & concurrency lock plugin for DSH",
  "type": "module",
  "main": "lib/index.js",
  "types": "lib/types/collab.d.ts",
  "exports": {
    ".": {
      "types": "./lib/types/collab.d.ts",
      "default": "./lib/index.js"
    },
    "./client": {
      "default": "./lib/client.js"
    },
    "./package.json": "./package.json"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    },
    "client": {
      "platform": "web",
      "inject": []
    }
  },
  "files": [
    "lib/**/*.js",
    "lib/**/*.d.ts",
    "skills",
    "cordis.patch.yml",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.client.json && cp -R src/types lib/",
    "build:types": "tsc --declaration --emitDeclarationOnly",
    "prepack": "npm run build",
    "test": "npm run build && node tests/collab-pure-logic.mjs && node tests/collab-integration.mjs && node tests/collab-hostcode-parity.mjs && node tests/collab-digest-stability.mjs && node tests/collab-awareness.mjs && node tests/collab-access-gate.mjs && node tests/collab-readers-push.mjs && node tests/collab-e2e.mjs && node tests/collab-skill.mjs && node tests/collab-client-route.mjs",
    "test:types": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@deepseek-ai/schemastery": "^3.18.2"
  },
  "peerDependencies": {
    "@deepseek-ai/dsh-tools": "^0.1.5-rc.1"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.9.3"
  },
  "keywords": [
    "dsh",
    "cordis",
    "multi-agent",
    "collaboration",
    "advisory-lock"
  ],
  "author": "",
  "license": "MIT"
}
