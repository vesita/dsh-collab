[package]
name = "collab-cli"
version = "0.3.0"
edition = "2021"
description = "High-performance CLI & Git-aware collaboration tool for DSH multi-agent workspace"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
anyhow = "1.0"
chrono = "0.4"
